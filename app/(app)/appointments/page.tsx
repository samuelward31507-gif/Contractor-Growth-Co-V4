import { PlaceholderPage } from "../_components/placeholder";

export default function AppointmentsPage() {
  return (
    <PlaceholderPage
      title="Appointments"
      description="See and manage every scheduled visit and estimate. Appointment scheduling is coming in a future update."
    />
  );
}
